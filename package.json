{
  "dependencies": {
    "three": "^0.170.0",
    "tween": "^0.9.0"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0",
    "vite": "^5.4.11"
  },
  "scripts": {
    "dev": "vite",
    "build:staging": "env-cmd -f .env.staging npm run build",
    "predeploy":"npm run build",
    "deploy":"gh-pages -d dist"
  }
}
